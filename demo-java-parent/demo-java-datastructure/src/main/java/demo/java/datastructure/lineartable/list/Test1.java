package demo.java.datastructure.lineartable.list;

/**
 * 一个 奇数位 升序  偶数位 降序的 链表 排序
 * <p>
 * 1. 将链表 拆分成 2 个链表  奇数 ，  偶数
 * <p>
 * 2. 偶数位的 链表 进行 翻转
 * <p>
 * 3. 链表 合并
 */
public class Test1 {

    public static ListNode[] getLists(ListNode head) {
        ListNode first = head;
        ListNode second = head;

        while (head.next != null && head.next.next != null) {
            // first =
        }

        return null;
    }
}
